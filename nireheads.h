#include <stdlib.h>
#include <stdio.h>
#include <stdarg.h> //variable argument list handlers
#include <stdint.h> //defines uintXX_t integers
#include <string.h>
#include <err.h>
#include <math.h> // -lm flag 
#include <time.h> // -lrt
#include <limits.h> //system specific integer/floating point/character sizes

#define max(a,b) (((a)>(b))?(a):(b))
#define min(a,b) (((a)>(b))?(b):(a))
